var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#public_api","page":"API","text":"BornSampling exports BornSampler and bornsample!.","title":"Public API"},{"category":"section","location":"api/#Constructing-a-sampler","page":"API","text":"For an MPS, construction selects pure-state sampling and the configuration length is L = length(state).\n\nFor an MPO, purified=true selects the exact physical marginal and also returns L indices. purified=false selects the joint distribution and returns 2L indices ordered as\n\nx₁, x₂, …, xL, y₁, y₂, …, yL.\n\nA rank-3 site inside an MPO contributes the synthetic purification index yᵢ = 1. This gives mixed rank-3/rank-4 MPOs one consistent configuration layout.\n\nFor a FiniteMPSTangents.TangentMPS, the sampler represents the coherent sum of all one-site insertion terms under the tangent-space/Hilbert-space isomorphism. With purified=true, local MPO purification legs and the persistent global tangent-symmetry leg are traced, and the configuration contains L physical indices. With purified=false, all existing local purification legs and the one global leg are sampled. The layout is\n\nx₁, …, xL [, y₁, …, yL] [, q].\n\nThe y group is present when any base tensor is rank four; rank-three sites in such a mixed base contribute yᵢ = 1. The final q entry is present only when the tangent tensors carry the extra persistent leg. The tangent and its base point are not mutated or canonicalized. The compiled sampler retains views into their tensor blocks, so they must not be modified while it is in use.\n\nleft_boundary accepts a pure boundary vector for a supported nontrivial left boundary. Sampler construction normalizes that vector, compiles the local contractions, and allocates reusable worker workspaces. Direct MPS/MPO inputs are additionally canonicalized at site 1.","title":"Constructing a sampler"},{"category":"section","location":"api/#Drawing-samples","page":"API","text":"The allocation-conscious single-shot form writes into a caller-owned vector:\n\nconfig = Vector{Int}(undef, length(state))\nlogp = BornSampling.bornsample!(rng, sampler, config)\n\nThe allocating convenience form returns a named tuple:\n\nshot = BornSampling.bornsample!(rng, sampler)\n# (configuration = ..., log_probability = ...)\n\nFor batched sampling:\n\nbatch = BornSampling.bornsample!(\n    rng,\n    sampler,\n    nshots;\n    ntasks=Threads.nthreads(),\n    disk=false,\n    maxsize=ntasks,\n)\n\nbatch.configuration stores one shot per column and batch.log_probability[n] is the log probability of column n. ntasks chooses the number of Julia worker tasks. Shots advance one site at a time and are dynamically assigned within each layer. With disk=true, maxsize chooses how many environments remain resident in each prefix frontier; the adjacent current and next frontiers coexist while a layer is being completed. For a tangent sampler, disk=true also gives its once-per-batch right suffix sweep a separate consume-once disk store. The first active completion stays in memory; each later suffix environment is loaded once, shared read-only by all workers at a layer, and deleted after the layer barrier. This suffix policy is controlled only by disk; maxsize still applies solely to prefix frontiers.\n\nAn ordinary probability is obtained when needed with exp(shot.log_probability) or exp.(batch.log_probability).","title":"Drawing samples"},{"category":"section","location":"api/#Direct-state-convenience","page":"API","text":"An MPS, MPO, or TangentMPS may be passed directly:\n\nshot = BornSampling.bornsample!(rng, deepcopy(state))\nbatch = BornSampling.bornsample!(rng, deepcopy(state), nshots; ntasks=4)\n\nThese forms construct a temporary sampler. Direct MPS/MPO forms canonicalize the supplied state, whereas the TangentMPS form leaves the tangent and its base unchanged. Reusing an explicit BornSampler retains its compiled plans, prefix-ready metadata, and numerical workspaces across calls.","title":"Direct state convenience"},{"category":"section","location":"api/#Configuration-basis","page":"API","text":"Every physical index is a one-based flat index in the corresponding BornSampling.TK physical-space canonical basis. Within a sector, the irrep index changes fastest and the degeneracy index changes next. Joint MPO configurations use the same convention for each purification index.\n\nThe exclamation mark records the public mutations: the core form writes config, every sampler call reuses mutable numerical workspaces, and direct MPS/MPO forms canonicalize their input state. Direct TangentMPS forms do not canonicalize or mutate the tangent or its base.","title":"Configuration basis"},{"category":"type","location":"api/#BornSampling.BornSampler","page":"API","text":"BornSampler(state::Union{FiniteMPS.MPS,FiniteMPS.MPO};\n            left_boundary=nothing, purified=true)\n\nCompile an in-memory FiniteMPS.MPS or FiniteMPS.MPO for repeated sequential Born sampling. An MPS is sampled as a rank-three physical amplitude. For an MPO, the default purified=true traces every rank-four tensor's first domain leg and samples the exact physical marginal. With purified=false, that leg is sampled jointly with the physical leg. Rank-three sites inside an MPO use a one-dimensional synthetic purification leg in either mode.\n\nConstruction calls FiniteMPS.canonicalize!(state, 1) in place, compiles all fusion-tree contraction plans, and allocates one reusable workspace. The sampler keeps views into state, so the state and its tensor data must not be modified after construction.\n\nThe left virtual space must contain one original sector with reduced multiplicity one. If its full dimension exceeds one, left_boundary must provide the pure state inside that sector. Mixed boundaries and coherent superpositions across residual charges are not supported.\n\nOne public batch call may use multiple internal worker tasks, each with an independent workspace. Concurrent external calls using the same sampler are not synchronized and are the caller's responsibility.\n\n\n\n\n\n","title":"BornSampling.BornSampler"},{"category":"function","location":"api/#BornSampling.bornsample!","page":"API","text":"bornsample!(rng, sampler::BornSampler, config::AbstractVector{Int}) -> Float64\n\nDraw one physical configuration into the caller-owned config vector and return its log probability. For an MPS or an MPO in the default traced-purification mode, config[i] is the one-based flat index in site i's TensorKit physical-space canonical basis. An MPO compiled with purified=false instead expects length 2L, stores all physical indices in config[1:L], all sampled purification indices in config[(L + 1):(2L)], and returns the joint log probability.\n\nThe method mutates the caller's config and the sampler's reusable numerical workspace. It does not alter the canonicalized state tensors.\n\n\n\n\n\nbornsample!(rng, sampler::BornSampler)\n\nDraw one sample and return its physical flat-index configuration and log probability. Recover the ordinary probability with exp(shot.log_probability) when it is needed. The canonical-basis metadata is already compiled in sampler.plans and is not duplicated in the result.\n\n\n\n\n\nbornsample!(rng, sampler::BornSampler, nshots::Int;\n            ntasks=Threads.nthreads(), disk=false, maxsize=ntasks)\n\nDraw a batch of samples using layer-synchronous sampled-prefix frontiers. Within each site, worker tasks dynamically claim shots and independently advance them. A layer barrier then releases the preceding frontier before the next site starts. Every worker owns an independent contraction workspace, while every shot retains its own RNG across layers. ntasks is not capped by the number of Julia threads; Julia's scheduler multiplexes the requested tasks. The caller must not invoke another sampling method on the same sampler concurrently.\n\nFor an MPS or an MPO in the default mode, the returned configuration matrix has shape (length(state), nshots), with one physical configuration per column. For an MPO compiled with purified=false, it has shape (2 * length(state), nshots): the first L rows are physical indices and the final L rows are purification indices. When disk=true, metadata stays in memory while larger node environments in each frontier are managed by a strict probability top-maxsize resident cache and raw temporary files. An MPS or joint-MPO node stores one normalized collapsed factor. A traced-MPO node instead stores the complete bank of uncompressed next-physical-outcome factors; an outcome is moved out of that bank and compressed only when its child edge is first used. A tangent batch additionally builds one q-resolved right suffix sweep for the whole call. With disk=true, those deterministic per-layer completions use a separate consume-once store: each is loaded before the layer barrier and deleted after use. maxsize controls only prefix-frontier residency, not tangent completion storage.\n\n\n\n\n\nbornsample!(rng, state::Union{FiniteMPS.MPS,FiniteMPS.MPO};\n            left_boundary=nothing, purified=true)\n\nCompile state and draw one sample. This convenience method modifies the input state's canonical gauge. Reuse a BornSampler for repeated shots.\n\n\n\n\n\n","title":"BornSampling.bornsample!"},{"category":"section","location":"#BornSampling.jl","page":"Home","text":"This repository is a downstream package of FiniteMPS.jl for drawing canonical-basis snapshots of MPS and MPO according to their Born probabilities. It outputs the sampled configurations and their log probabilities for subsequent statistical analysis.","title":"BornSampling.jl"},{"category":"section","location":"#First-sample","page":"Home","text":"import BornSampling\nusing BornSampling.FiniteMPS: randMPS, ℂ\nusing BornSampling.Random: MersenneTwister, seed!\n\nseed!(1234)\nstate = randMPS(4, ℂ^2, ℂ^1)\nsampler = BornSampling.BornSampler(state)\n\nrng = MersenneTwister(1234)\nshot = BornSampling.bornsample!(rng, sampler)\n\n(\n    configuration = shot.configuration,\n    log_probability = shot.log_probability,\n)\n(configuration = [2, 2, 1, 1], log_probability = -2.9663403242621476)\n\nSampler construction canonicalizes state in place. The compiled sampler then owns that canonicalized tensor data and can be reused for many shots.\n\nContinue with the Tutorial, Implementation, and Public API.\n\nPages = [\"tutorial.md\", \"implementation.md\", \"api.md\"]\nDepth = 2","title":"First sample"},{"category":"section","location":"implementation/#implementation","page":"Implementation","text":"BornSampling is organized around one left-to-right factor propagation.  The probability model determines the sampling mode, while compiled local plans provide the symmetry-aware contractions used by every mode.","title":"Implementation"},{"category":"section","location":"implementation/#Probability-model","page":"Implementation","text":"","title":"Probability model"},{"category":"section","location":"implementation/#Pure-MPS-amplitudes","page":"Implementation","text":"For an MPS, each local tensor has the canonical order\n\nA_iell_ix_ir_i\n\nand the contracted network defines an amplitude psi(boldsymbolx). BornSampling draws the physical configuration from\n\np(boldsymbolx)\n= fracpsi(boldsymbolx)^2langlepsipsirangle","title":"Pure MPS amplitudes"},{"category":"section","location":"implementation/#Purification-amplitudes","page":"Implementation","text":"An MPO is interpreted as a pure amplitude on a physical space and a purification space.  A rank-four local tensor has the order\n\nX_iell_ix_iy_ir_i\n\nand a rank-three local tensor inside the same MPO supplies the synthetic value y_i=1.  The complete network defines X(boldsymbolxboldsymboly) and hence the joint distribution\n\np(boldsymbolxboldsymboly)\n= fracX(boldsymbolxboldsymboly)^2\n        langle XXrangle\n\nThe two MPO modes follow directly from this distribution:\n\nbeginaligned\ntexttraced modequad\np(boldsymbolx)\n  = sum_boldsymbolyp(boldsymbolxboldsymboly)\ntextjoint modequad\n(boldsymbolxboldsymboly)\n  sim p(boldsymbolxboldsymboly)\nendaligned\n\nThus discarding boldsymboly from joint samples also produces samples distributed according to the exact physical marginal p(boldsymbolx).","title":"Purification amplitudes"},{"category":"section","location":"implementation/#Tangent-vector-states","page":"Implementation","text":"A FiniteMPSTangents.TangentMPS represents the Hilbert-space state\n\nPhi_boldsymbol yqrangle\n=sum_j=1^L\nA^l_1cdots A^l_j-1B_jqA^r_j+1cdots A^r_L\n\nThe insertion-site sum is coherent: all same-q cross terms are retained. The extra q leg is one persistent global label, not a virtual bond and not a distinct local outcome at every site. Traced tangent sampling uses\n\nrho=sum_boldsymbol yq\nPhi_boldsymbol yqranglelanglePhi_boldsymbol yq\n\nwhereas joint tangent sampling draws (boldsymbol xboldsymbol yq). A synthetic first sampling layer draws q once when that global leg exists; site layers then propagate only the selected q. The external configuration remains ordered as boldsymbol xboldsymbol yq.","title":"Tangent-vector states"},{"category":"section","location":"implementation/#Sequential-probabilities","page":"Implementation","text":"At site i, let a_i denote the sampled local outcome: x_i in MPS and traced-MPO modes, or (x_iy_i) in joint-MPO mode.  The contraction produces nonnegative branch weights q_i(a_i) and\n\nz_i = sum_a_iq_i(a_i) qquad\np(a_imid a_1ldotsa_i-1) = fracq_i(a_i)z_i\n\nThe returned log probability is accumulated directly from conditional log weights,\n\nlog p(a_1ldotsa_L)\n= sum_i=1^Lleftlog q_i(a_i)-log z_iright","title":"Sequential probabilities"},{"category":"section","location":"implementation/#Unbiased-estimates-from-samples","page":"Implementation","text":"For N independent physical samples boldsymbolx^(s), the empirical probability of a fixed configuration boldsymbolx_0 is\n\nwidehat p(boldsymbolx_0)\n= fracN_boldsymbolx_0N\n= frac1Nsum_s=1^N\n   mathbf1leftboldsymbolx^(s)=boldsymbolx_0right\n\nIts expectation is\n\nmathbbEwidehat p(boldsymbolx_0)\n= frac1Nsum_s=1^N\n   mathbbPleft(boldsymbolx^(s)=boldsymbolx_0right)\n= p(boldsymbolx_0)\n\nThe same derivation applies after retaining only boldsymbolx from joint samples, because\n\nmathbbP(boldsymbolx^(s)=boldsymbolx_0)\n= sum_boldsymbolyp(boldsymbolx_0boldsymboly)\n= p(boldsymbolx_0)\n\nFor an observable diagonal in the sampled basis, with value O(boldsymbolx), the sample mean\n\nwidehatlangle Orangle\n= frac1Nsum_s=1^NO(boldsymbolx^(s))\n\nis unbiased since\n\nmathbbEwidehatlangle Orangle\n= sum_boldsymbolxp(boldsymbolx)O(boldsymbolx)\n= langle Orangle\n\nWith the usual unbiased sample variance s_O^2, s_O^2N estimates the variance of this mean and s_OsqrtN supplies its standard error.","title":"Unbiased estimates from samples"},{"category":"section","location":"implementation/#Factorized-boundary-propagation","page":"Implementation","text":"After canonicalizing the state at site 1, the sampler needs only the collapsed left environment.  It stores that environment as a factor\n\nrho_i = C_i C_i^dagger\nqquad\noperatornametr(C_i C_i^dagger)=lVert C_irVert_F^2=1\n\nThe initial pure boundary gives a factor with one auxiliary column.  For a fixed local physical and purification basis value, define\n\nK_i(xy)rell=X_iellxyr\nqquad\nY_i(xy)=K_i(xy)C_i","title":"Factorized boundary propagation"},{"category":"section","location":"implementation/#Traced-MPO-mode","page":"Implementation","text":"During the weight pass, every physical outcome receives an uncompressed factor whose purification outcomes become distinct columns:\n\nG_i(x)=\nbeginbmatrix\nY_i(x1)  Y_i(x2)  cdots  Y_i(xd_y)\nendbmatrix\n\nThe node stores the complete environment bank mathcalG_i=(G_i(1)ldotsG_i(d_x)) and obtains every branch weight from that same pass:\n\nq_i(x)=lVert G_i(x)rVert_F^2\n=sum_ylVert K_i(xy)C_irVert_F^2\n\nWhen an outcome x first creates a child edge, that child takes the already stored G_i(x) from the bank and normalizes it by sqrtq_i(x). A residual-symmetry block with more columns than right-space rows receives the exact LQ factorization G_i=L_iQ_i at this point. Since Q_iQ_i^dagger=I,\n\nG_iG_i^dagger=L_iL_i^dagger\n\nso replacing that block by L_i preserves the density factor exactly and bounds its column count by the right bond dimension. Each resident traced node therefore reserves space for all physical-branch factors; this larger environment payload enables one-pass weight construction and immediate reuse by every child that is reached.","title":"Traced MPO mode"},{"category":"section","location":"implementation/#Pure-and-joint-modes","page":"Implementation","text":"An MPS has d_y=1 and begins with one factor column, so the update remains a single ket column throughout the chain.  Joint MPO sampling also selects one specific pair (xy) at each site; its update is\n\nC_i+1=fracK_i(xy)C_isqrtq_i(xy)\n\nand likewise remains a pure, rank-one factor.  These modes therefore inherit the usual dense-equivalent O(D^2) sequential contraction, while tracing a purification can raise the factor rank to D and gives O(D^3) worst-case work.  In every mode the environment remains represented lazily by C_iC_i^dagger.\n\nFor tangent joint sampling, fixing the synthetic global q root and one local y_i at every site likewise keeps the history factor rank one. The hot path applies each compiled residual-block route directly to the U and V_q factor columns, without materializing a dense local channel. Thus the fixed-q MPS/MPO joint propagation has the same dense-equivalent O(D^2) character. Traced local purification may require a common exact history compression and has O(D^3) worst-case work.","title":"Pure and joint modes"},{"category":"section","location":"implementation/#Global-modes-and-local-tensor-ranks","page":"Implementation","text":"The outer BornSampling.FiniteMPS concrete type chooses the global semantics:\n\ninput and constructor sampled outcome factor update\nBornSampling.FiniteMPS.MPS x_i pure rank-one propagation\nBornSampling.FiniteMPS.MPO, purified=true x_i purification trace and exact factor compression\nBornSampling.FiniteMPS.MPO, purified=false (x_iy_i) joint rank-one propagation\nFiniteMPSTangents.TangentMPS, purified=true x_i common (UV_q) history propagation\nFiniteMPSTangents.TangentMPS, purified=false one global q, then (x_iy_i) fixed-q rank-one history propagation\n\nConstruction checks once that every tensor in an MPS is rank three.  An MPO may mix rank-three and rank-four sites.  At a rank-three MPO site, the local purification basis is the singleton y_i=1; a traced MPO still carries any factor rank accumulated at earlier rank-four sites.  This division keeps the probability model global while local static rank controls only leg access and the contraction kernel.","title":"Global modes and local tensor ranks"},{"category":"section","location":"implementation/#Compiled-symmetry-contractions","page":"Implementation","text":"Each site is compiled into a SitePlan.  The plan records full-basis metadata, views of the reduced TensorMap blocks, allowed sector transitions, and cached fusion-tree kernels.  A physical flat index resolves to (sector degeneracy irrep) in TensorKit's canonical array order, so fixing a sampled basis vector fixes both its degeneracy and irrep coordinates.","title":"Compiled symmetry contractions"},{"category":"section","location":"implementation/#Residual-symmetry","page":"Implementation","text":"Fixing local basis vectors can leave only part of the original symmetry unbroken.  Compilation infers this residual symmetry structurally.  A one-dimensional UniqueFusion component is retained as a residual charge; in a product symmetry this decision is made component by component.  Remaining components, including non-Abelian carrier coordinates, are folded into the row degeneracy of the residual TensorMap.\n\nEvery original virtual sector is embedded into a residual sector block.  Local transitions with the same residual input and output charges form one ChannelRoute, and their amplitudes are accumulated coherently before taking a norm.  This representation preserves charge block sparsity while retaining the interference between original sectors that share a residual charge.","title":"Residual symmetry"},{"category":"section","location":"implementation/#Unique-and-fusion-tree-paths","page":"Implementation","text":"The contraction style is selected once through BornSampling.TK.FusionStyle(sector_type):\n\nUniqueStyle contracts one-dimensional irrep carriers directly from the reduced block view with matrix multiplication.\nFusionTreeStyle caches convert(Array, (fout, fin)) during compilation. At sampling time it slices the reduced block and the cached fusion kernel, then contracts their degeneracy and irrep coordinates with preallocated scratch space.\n\nBoth paths propagate the ket with ordinary transpose; conjugation enters through Frobenius norms and CC^dagger.  Rank-three and rank-four methods supply the corresponding reduced and fusion-kernel slices, while the site loop and probability calculation remain shared.","title":"Unique and fusion-tree paths"},{"category":"section","location":"implementation/#Layer-synchronous-prefix-reuse","page":"Implementation","text":"A batched call advances every shot one site per layer. The current frontier stores the sampled prefixes entering that site; a separate next frontier receives the children selected there. Each node carries its prefix log probability, next-site branch weights, child slots, and TensorMap space metadata for its numerical environment. MPS and joint-MPO nodes own one normalized rank-one factor. Traced-MPO nodes own the complete uncompressed G_x bank produced by their weight pass. Shots that reach the same prefix reuse its branch weights and environment payload.\n\nWithin a layer, a shared atomic counter dynamically assigns shots to workers. Every worker has an independent numerical workspace, and every shot retains its own seeded RNG across layers. The seed sequence is drawn from the caller's RNG before work starts, so scheduling does not change the sampled result. ntasks controls the number of Julia tasks up to the number of shots; Julia schedules those tasks over the available threads.\n\nAfter all shots finish a layer, the barrier makes the next frontier complete. The preceding frontier's metadata, environments, and temporary files are then released before the next site starts. A frontier contains at most N nodes; during a transition only the adjacent current and next frontiers coexist.\n\nParallel publication uses narrow locks:\n\nevery child edge has an atomic node id and a lock used only while its first worker constructs that child;\nan existing edge uses its published id through the lock-free read path;\nimmutable node metadata is fully initialized before the id is published;\neach worker writes its own output column and uses its own contraction workspace.\n\nThis synchronization belongs to one batched bornsample! invocation. The sampler's public concurrency contract leaves coordination between simultaneous external invocations to the caller.","title":"Layer-synchronous prefix reuse"},{"category":"section","location":"implementation/#Tangent-suffix-completions","page":"Implementation","text":"Before a nonempty tangent batch enters the shared left-to-right scheduler, it performs one right-to-left sweep of q-resolved quadratic environments:\n\nI=R^dagger Rqquad K_q=R^dagger T_qqquad\nN_q=T_q^dagger T_q\n\nThese matrices complete the sampled prefix weight without expanding the Ltimes L insertion-site cross terms. Each metric stores only its nonzero residual-sector block pairs, including the possibly charge-shifting pairs of K_q; each transfer visits only compatible channel/metric blocks. The setup therefore has blockwise cubic cost (worst-case dense equivalent O(LQD^3)) once per batch, not once per shot. Every site completion is then consumed exactly once by the outer layer scheduler and shared read-only by all workers. The same-site B^dagger I B term enters once; the two K_q orientations account for distinct-site cross terms without double counting.\n\nWith disk=false, pending completions live in batch memory. With disk=true, the first active completion stays in memory and the right sweep serializes the rest. The scheduler loads one completion before its layer barrier, and that file is immediately deleted. Active right-environment memory is therefore constant in chain length; all remaining files and references are removed by the batch finally cleanup. This deterministic completion store is separate from the probability-ranked prefix cache and has no maxsize policy.","title":"Tangent suffix completions"},{"category":"section","location":"implementation/#Probability-ranked-environment-storage","page":"Implementation","text":"Prefix metadata and branch weights stay in memory for their frontier. Larger numerical environments are managed separately. With disk storage enabled, every frontier maintains its own probability-ranked set of at most maxsize resident environments: one normalized factor for an MPS or joint-MPO node, or one complete branch bank for a traced-MPO node. Other environments in that frontier are stored as raw TensorMap records.\n\nA new environment enters its frontier's resident set only when its prefix probability is strictly greater than the current minimum; exact ties stay on disk. Because the next frontier is ranked independently, its final resident set is the exact top-maxsize subset at that depth. While a layer transition is in progress, the adjacent frontiers can together hold up to 2textttmaxsize resident environments. After the barrier releases the preceding frontier, the active set is again bounded by maxsize.\n\nThe source frontier's metadata and resident dictionary remain fixed while its environments are read or consumed and the destination frontier is constructed. An admission lock serializes destination dictionary updates and top-set replacement. Each raw TensorMap record is published through a temporary file followed by an atomic rename, and immutable space metadata reconstructs a rank-one factor or a traced branch-bank member. Removing the source frontier at the layer boundary also removes its temporary directory.","title":"Probability-ranked environment storage"},{"category":"section","location":"tutorial/#tutorial","page":"Tutorial","text":"This tutorial samples a half-filled Hubbard chain from a ground-state MPS and a thermal purification MPO. The MPS produces physical-basis snapshots; the MPO demonstrates both the exact physical marginal and the joint physical–purification distribution.\n\nThe examples use a chain of length L=16, U=8, mu=U2, and bond dimension D=512. Documentation builds execute every block online, so this compact system and bond dimension keep the build practical. The calculations demonstrate the workflow and do not guarantee bond-dimension or sweep convergence.","title":"Tutorial"},{"category":"section","location":"tutorial/#tutorial_setup","page":"Tutorial","text":"The Hamiltonian is\n\nH = -sum_langle ijranglesigma\nleft(c^dagger_isigmac_jsigma + mathrmhcright)\n+ Usum_i n_iuparrown_idownarrow - musum_i n_i\n\nThe charge U(1) and spin SU(2) representation comes from BornSampling.FiniteMPS; reduced TensorKit spaces are available through BornSampling.TK. Model construction, state preparation, direct contraction, and plotting live in tutorial_helpers.jl, leaving the sampling path visible here.\n\nL = 16\nU = 8.0\nmu = U / 2\nD = 512\nNs_rankone = 1000\nNs_traced = 100\ntraced_batch_count = 10\nntasks = Threads.nthreads()\n\nhamiltonian = hubbard_hamiltonian(L; U, mu)\nsz_lookup = hubbard_sz_lookup()\n4-element Vector{Float64}:\n  0.5\n -0.5\n  0.0\n  0.0\n\nFor every curve, pairs at the same integer separation r=j-i are averaged inside each shot. Those per-shot radial values determine the sample mean and its standard error.","title":"Shared Hubbard setup"},{"category":"section","location":"tutorial/#tutorial_mps","page":"Tutorial","text":"At half filling the total U(1) charge is zero in the particle–hole-centered convention. A spin-singlet boundary initializes the MPS, and CBE 1-DMRG prepares the state at bond-dimension ceiling D.\n\nground_state, ground_result = prepare_ground_state(\n    hamiltonian;\n    nsites=L,\n    D,\n    seed=3101,\n)\nground_result\n(sweeps = 3, final_energy = -69.03713409165385)\n\nBornSampling.BornSampler compiles the canonical-basis contraction plans. The batched call returns one configuration per matrix column together with its log probability.\n\nground_direct = direct_szsz_by_distance(ground_state; ntasks)\n\nground_sampler = BornSampling.BornSampler(ground_state)\nground_batch = BornSampling.bornsample!(\n    Random.MersenneTwister(3102),\n    ground_sampler,\n    Ns_rankone;\n    ntasks,\n)\n\nground_mean, ground_se = sampled_szsz_by_distance(\n    ground_batch.configuration,\n    sz_lookup,\n)\n\nplot_szsz_comparison(\n    ground_direct,\n    ((\n        mean=ground_mean,\n        standard_error=ground_se,\n        color=:royalblue,\n        label=\"MPS samples\",\n    ),);\n    direct_label=\"Ground-state expectation\",\n    filename=\"tutorial_hubbard_ground_state_szsz.png\",\n)\n\n(Image: )\n\nThe sample mean estimates the diagonal spin correlation, and the whiskers show its standard error. The Hamiltonian is retained for thermal preparation.\n\nground_batch = nothing\nground_sampler = nothing\nground_state = nothing\nGC.gc()","title":"Ground-state MPS sampling"},{"category":"section","location":"tutorial/#tutorial_mpo","page":"Tutorial","text":"For a thermal factor Xrangle, purified=true draws the exact physical marginal\n\np(x) = fracsum_y X(xy)^2langle XXrangle\n\nwhile purified=false draws the joint distribution\n\np(xy) = fracX(xy)^2langle XXrangle\n\nSelecting the physical part of a joint sample gives the same marginal p(x). SETTN initializes the thermal factor at high temperature, and CBE 1-TDVP evolves it on an exponential grid to beta=1.\n\nfactor, beta = prepare_thermal_factor(hamiltonian; D)\nbeta\n1.0","title":"Thermal purification MPO sampling"},{"category":"section","location":"tutorial/#Exact-physical-marginal","page":"Tutorial","text":"The direct thermal contraction provides the reference curve. The traced sampler then produces physical configurations from the exact marginal. Running the traced shots as several small batches trades lower peak prefix-bank memory for less parallel work and prefix reuse per call.\n\nthermal_direct = direct_szsz_by_distance(factor; ntasks)\n\ntraced_state = deepcopy(factor)\ntraced_sampler = BornSampling.BornSampler(traced_state; purified=true)\ntraced_rng = Random.MersenneTwister(3201)\ntraced_configuration = Matrix{Int}(undef, L, Ns_traced)\ntraced_batch_size = Ns_traced ÷ traced_batch_count\n\nfor batch in 1:traced_batch_count\n    columns = ((batch - 1) * traced_batch_size + 1):(batch * traced_batch_size)\n    result = BornSampling.bornsample!(\n        traced_rng,\n        traced_sampler,\n        traced_batch_size;\n        ntasks,\n    )\n    traced_configuration[:, columns] .= result.configuration\nend\n\ntraced_mean, traced_se = sampled_szsz_by_distance(\n    traced_configuration,\n    sz_lookup,\n)\n\ntraced_configuration = nothing\ntraced_sampler = nothing\ntraced_state = nothing\nGC.gc()","title":"Exact physical marginal"},{"category":"section","location":"tutorial/#Joint-physical–purification-samples","page":"Tutorial","text":"Joint sampling returns a 2Ltimes N_s configuration matrix ordered as x_1ldotsx_Ly_1ldotsy_L. The physical rows feed the same diagonal observable estimator.\n\njoint_sampler = BornSampling.BornSampler(factor; purified=false)\njoint_batch = BornSampling.bornsample!(\n    Random.MersenneTwister(3202),\n    joint_sampler,\n    Ns_rankone;\n    ntasks,\n)\njoint_physical_configuration = @view joint_batch.configuration[1:L, :]\njoint_mean, joint_se = sampled_szsz_by_distance(\n    joint_physical_configuration,\n    sz_lookup,\n)\n\njoint_summary = (;\n    joint_configuration_size=size(joint_batch.configuration),\n    samples=Ns_rankone,\n)\njoint_summary\n(joint_configuration_size = (32, 1000), samples = 1000)\n\nThe direct contraction, traced marginal samples, and physical part of the joint samples form the three thermal comparison curves.\n\nplot_szsz_comparison(\n    thermal_direct,\n    (\n        (\n            mean=traced_mean,\n            standard_error=traced_se,\n            color=:royalblue,\n            label=\"Trace (×$(Ns_traced))\",\n        ),\n        (\n            mean=joint_mean,\n            standard_error=joint_se,\n            color=:darkorange,\n            label=\"Joint (×$(Ns_rankone))\",\n        ),\n    );\n    direct_label=\"Thermal expectation\",\n    filename=\"tutorial_hubbard_thermal_szsz.png\",\n)\n\n(Image: )","title":"Joint physical–purification samples"}]
}
