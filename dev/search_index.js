var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#public_api","page":"API","text":"BornSampling exports BornSampler and bornsample!.","title":"Public API"},{"category":"section","location":"api/#Constructing-a-sampler","page":"API","text":"For an MPS, purified=true traces the left boundary and returns L = length(state) physical indices. With purified=false, the boundary index is sampled jointly, returning [x₁, …, xL, b] of length L + 1.\n\nFor an MPO, purified=true traces the left boundary and local purification legs, returning L physical indices. With purified=false, physical, purification, and boundary indices are sampled jointly, returning 2L + 1 entries ordered as:\n\nx₁, x₂, …, xL, y₁, y₂, …, yL, b.\n\nAt rank-three sites lacking a purification leg, yᵢ is set to 0.\n\nFor a FiniteMPSTangents.TangentMPS, the sampler represents the coherent sum of one-site insertion terms. With purified=true, boundary, purification, and global tangent legs are traced, returning L physical indices. With purified=false, all indices are sampled jointly with layout:\n\nx₁, …, xL [, y₁, …, yL], b [, q].\n\nThe y group is included when the base state contains rank-four tensors (with yᵢ = 0 at rank-three sites). The q entry is included when the tangent carries a global symmetry leg. Constructing a tangent sampler retains views into the underlying tensors without modifying them.\n\nConstructing a BornSampler compiles local contractions, allocates reusable workspaces, and canonicalizes MPS/MPO inputs at site 1.","title":"Constructing a sampler"},{"category":"section","location":"api/#Drawing-samples","page":"API","text":"The in-place single-shot form writes into a caller-provided vector:\n\nconfig = Vector{Int}(undef, length(state))\nlogp = BornSampling.bornsample!(rng, sampler, config)\n\nFor joint sampling, size config according to the joint configuration layout.\n\nThe allocating single-shot form returns a named tuple:\n\nshot = BornSampling.bornsample!(rng, sampler)\n# (configuration = ..., log_probability = ...)\n\nFor batched sampling:\n\nbatch = BornSampling.bornsample!(\n    rng,\n    sampler,\n    nshots;\n    ntasks=Threads.nthreads(),\n    disk=false,\n    maxsize=ntasks,\n)\n\nbatch.configuration stores one shot per column, and batch.log_probability[n] is the log probability of column n. ntasks sets the number of concurrent Julia worker tasks. With disk=true, prefix environments are offloaded to disk, keeping at most maxsize environments in memory per layer. For tangent sampling, disk=true also buffers intermediate right-suffix environments to disk.\n\nProbabilities can be recovered with exp.(batch.log_probability).","title":"Drawing samples"},{"category":"section","location":"api/#Direct-state-convenience","page":"API","text":"An MPS, MPO, or TangentMPS may be passed directly:\n\nshot = BornSampling.bornsample!(rng, deepcopy(state))\nbatch = BornSampling.bornsample!(rng, deepcopy(state), nshots; ntasks=4)\n\nThese convenience methods construct a temporary sampler. Reusing an explicit BornSampler avoids recompilation and workspace reallocation across repeated calls.","title":"Direct state convenience"},{"category":"section","location":"api/#Configuration-basis","page":"API","text":"Sampled physical indices are 1-based flat indices in the TensorKit canonical basis, with irrep indices changing fastest, followed by degeneracy indices. Purification, boundary (b), and global tangent (q) indices follow the same canonical ordering. A value of 0 indicates an absent purification leg at a rank-three site.","title":"Configuration basis"},{"category":"type","location":"api/#BornSampling.BornSampler","page":"API","text":"BornSampler(state::Union{FiniteMPS.MPS,FiniteMPS.MPO}; purified=true)\n\nCompile an in-memory state of length L for repeated Born sampling. With purified=true, trace the full left boundary and every local purification leg, returning the physical marginal in [x₁, …, xL] order.\n\nWith purified=false, sample all these indices. An MPS returns [x₁, …, xL, b]; an MPO returns [x₁, …, xL, y₁, …, yL, b]. The boundary index b is sampled before the sites and is always returned, including b=1 for a one-dimensional boundary. Each rank-four MPO tensor's first domain leg supplies its local yᵢ. A rank-three site has no such leg and returns yᵢ=0; an actual one-dimensional purification leg returns yᵢ=1. All sampled indices use the one-based flat order of their TensorKit canonical bases.\n\nConstruction calls FiniteMPS.canonicalize!(state, 1) in place, compiles all fusion-tree contraction plans, and allocates one reusable workspace. The sampler keeps views into state, so the state and its tensor data must not be modified after construction.\n\nThe left virtual space must contain one original sector with reduced multiplicity one; its full irrep dimension may exceed one.\n\nOne public batch call may use multiple internal worker tasks, each with an independent workspace. Concurrent external calls using the same sampler are not synchronized and are the caller's responsibility.\n\n\n\n\n\n","title":"BornSampling.BornSampler"},{"category":"function","location":"api/#BornSampling.bornsample!","page":"API","text":"bornsample!(rng, sampler::BornSampler, config::AbstractVector{Int}) -> Float64\n\nDraw one configuration into the caller-owned config vector and return its log probability. With purified=true, config has length L and config[i] is the one-based flat index in site i's TensorKit physical-space canonical basis. With purified=false, an MPS expects length L+1 in [x; b] order, and an MPO expects length 2L+1 in [x; y; b] order. A tangent uses [x; optional y; b; optional q] as described by BornSampler. An absent purification leg contributes zero in a present y group; sampled basis indices start at one. The returned log probability includes every sampled index.\n\nThe method mutates the caller's config and the sampler's reusable numerical workspace. State tensors remain unchanged.\n\n\n\n\n\nbornsample!(rng, sampler::BornSampler)\n\nDraw one sample and return its flat-index configuration and log probability. The physical or joint layout follows BornSampler. Recover the ordinary probability with exp(shot.log_probability) when it is needed.\n\n\n\n\n\nbornsample!(rng, sampler::BornSampler, nshots::Int;\n            ntasks=Threads.nthreads(), disk=false, maxsize=ntasks)\n\nDraw a batch of samples using layer-synchronous sampled-prefix frontiers. Within each sampling layer, worker tasks dynamically claim shots, grouped by nonresident parent for MPS and joint-MPO batches. A layer barrier then releases the preceding frontier before the next layer starts. Every worker owns an independent contraction workspace, while every shot retains its own RNG across layers. ntasks is not capped by the number of Julia threads; Julia's scheduler multiplexes the requested tasks. The caller must not invoke another sampling method on the same sampler concurrently.\n\nIn the default traced mode, the returned configuration matrix has shape (L, nshots), with one physical configuration per column. With purified=false, an MPS returns (L+1, nshots) in [x; b] order, and an MPO returns (2L+1, nshots) in [x; y; b] order. A tangent uses the rank-dependent [x; optional y; b; optional q] layout of BornSampler. An absent local purification leg contributes zero in a present y group. When disk=true, metadata stays in memory while larger node environments in each frontier are managed by a strict probability top-maxsize resident cache and raw temporary files. An MPS or joint-MPO node stores one propagated factor. A traced-MPO node instead stores the complete bank of uncompressed next-physical-outcome factors; an outcome is moved out of that bank and compressed only when its child edge is first used. A tangent batch additionally builds one q-resolved right suffix sweep for the whole call. With disk=true, those deterministic per-layer completions use a separate consume-once store: each is loaded before the layer barrier and deleted after use. maxsize controls only prefix-frontier residency, not tangent completion storage.\n\n\n\n\n\nbornsample!(rng, state::Union{FiniteMPS.MPS,FiniteMPS.MPO}; purified=true)\n\nCompile state and draw one sample. This convenience method modifies the input state's canonical gauge. Reuse a BornSampler for repeated shots.\n\n\n\n\n\n","title":"BornSampling.bornsample!"},{"category":"section","location":"#BornSampling.jl","page":"Home","text":"BornSampling draws canonical-basis snapshots of MPS and MPO states according to their Born probabilities, based on FiniteMPS.jl. It returns the sampled configurations alongside their log probabilities.","title":"BornSampling.jl"},{"category":"section","location":"#First-sample","page":"Home","text":"import BornSampling\nusing BornSampling.FiniteMPS: randMPS, ℂ\nusing BornSampling.Random: MersenneTwister, seed!\n\nseed!(1234)\nstate = randMPS(4, ℂ^2, ℂ^1)\nsampler = BornSampling.BornSampler(state)\n\nrng = MersenneTwister(1234)\nshot = BornSampling.bornsample!(rng, sampler)\n\n(\n    configuration = shot.configuration,\n    log_probability = shot.log_probability,\n)\n(configuration = [2, 2, 1, 1], log_probability = -2.9663403242621476)\n\nConstructing a BornSampler canonicalizes state in place and compiles contraction plans that can be reused across repeated sampling calls.\n\nSee the Tutorial, Implementation, and Public API for details.\n\nPages = [\"tutorial.md\", \"implementation.md\", \"api.md\"]\nDepth = 2","title":"First sample"},{"category":"section","location":"implementation/#implementation","page":"Implementation","text":"BornSampling samples configurations using a single left-to-right factor propagation. The probability model dictates the sampling mode, while compiled local plans perform symmetry-aware contractions.","title":"Implementation"},{"category":"section","location":"implementation/#Probability-model","page":"Implementation","text":"","title":"Probability model"},{"category":"section","location":"implementation/#MPS-amplitudes-with-an-open-boundary","page":"Implementation","text":"For an MPS, local tensors follow the canonical index ordering\n\nA_iell_ix_ir_i\n\nand the contracted network defines the amplitude psi(bboldsymbolx), where b labels the left-boundary basis with irrep dimension d_b.\n\nWith purified=true, the boundary is traced out to sample physical configurations:\n\np(boldsymbolx)\n= fracsum_bpsi(bboldsymbolx)^2\n        sum_bboldsymbolxpsi(bboldsymbolx)^2\n\nWith purified=false, the boundary index b is sampled first from the joint distribution, returning boldsymbolxb of length L+1.","title":"MPS amplitudes with an open boundary"},{"category":"section","location":"implementation/#Purification-amplitudes","page":"Implementation","text":"An MPO is treated as a pure state defined over its open left boundary, physical spaces, and local purification spaces. A rank-four local tensor has index ordering\n\nX_iell_ix_iy_ir_i\n\nwhere x_i and y_i denote physical and purification indices, respectively. At rank-three sites lacking a purification leg, the output sets y_i=0. The complete network defines X(bboldsymbolxboldsymboly) and the joint distribution\n\np(boldsymbolxboldsymbolyb)\n= fracX(bboldsymbolxboldsymboly)^2\n        langle XXrangle\n\nThe two sampling modes correspond to:\n\nbeginaligned\ntexttraced modequad\np(boldsymbolx)\n  = sum_boldsymbolybp(boldsymbolxboldsymbolyb)\ntextjoint modequad\n(boldsymbolxboldsymbolyb)\n  sim p(boldsymbolxboldsymbolyb)\nendaligned\n\nSetting purified=true samples from the traced physical marginal p(boldsymbolx), returning L physical indices. Setting purified=false draws the boundary b followed by all physical and purification indices, returning boldsymbolxboldsymbolyb of length 2L+1. Discarding boldsymboly and b from joint samples recovers the exact physical marginal.","title":"Purification amplitudes"},{"category":"section","location":"implementation/#Tangent-vector-states","page":"Implementation","text":"A FiniteMPSTangents.TangentMPS represents a tangent state with open left boundary b:\n\nPhi_bboldsymbol yqrangle\n=leftsum_j=1^L\nA^l_1cdots A^l_j-1B_jqA^r_j+1cdots A^r_Lright_b\n\nwhere the sum over insertion sites is coherent, and q is an optional global symmetry leg.\n\nWith purified=true, the boundary, purification legs, and global leg are traced out:\n\nrho=sum_bboldsymbol yq\nPhi_bboldsymbol yqranglelanglePhi_bboldsymbol yq\n\nreturning L physical indices. With purified=false, b and q are sampled prior to site updates, returning boldsymbol xboldsymbol ybq. If the base state contains rank-four tensors, each site includes a y_i slot (recording 0 at rank-three sites); if all base tensors are rank three, the boldsymbol y group is omitted.","title":"Tangent-vector states"},{"category":"section","location":"implementation/#Sequential-probabilities","page":"Implementation","text":"At site i, let a_i denote the sampled outcome: x_i in traced or MPS modes, or (x_i y_i) at a rank-four joint-mode site. Contracting the site tensor against the current environment yields branch weights q_i(a_i):\n\nz_i = sum_a_iq_i(a_i) qquad\np(a_imid a_1ldotsa_i-1) = fracq_i(a_i)z_i\n\nThe physical sites contribute\n\nsum_i=1^Lleftlog q_i(a_i)-log z_iright\n\nto the total log probability. In joint mode, the conditional log probabilities of the initial boundary draw b (and global tangent leg q, if present) are included, giving the total log probability of the sampled configuration.","title":"Sequential probabilities"},{"category":"section","location":"implementation/#Unbiased-estimates-from-samples","page":"Implementation","text":"For N independent physical samples boldsymbolx^(s), the empirical probability of a configuration boldsymbolx_0 is\n\nwidehat p(boldsymbolx_0)\n= fracN_boldsymbolx_0N\n= frac1Nsum_s=1^N\n   mathbf1leftboldsymbolx^(s)=boldsymbolx_0right\n\nwhich is an unbiased estimator:\n\nmathbbEwidehat p(boldsymbolx_0)\n= frac1Nsum_s=1^N\n   mathbbPleft(boldsymbolx^(s)=boldsymbolx_0right)\n= p(boldsymbolx_0)\n\nThe same property holds for the physical marginal of joint samples, since\n\nmathbbP(boldsymbolx^(s)=boldsymbolx_0)\n= sum_boldsymbolybp(boldsymbolx_0boldsymbolyb)\n= p(boldsymbolx_0)\n\nFor an observable diagonal in the sampling basis with values O(boldsymbolx), the sample mean\n\nwidehatlangle Orangle\n= frac1Nsum_s=1^NO(boldsymbolx^(s))\n\nsatisfies mathbbEwidehatlangle Orangle = langle Orangle. Its standard error is estimated by s_OsqrtN, where s_O^2 is the sample variance.","title":"Unbiased estimates from samples"},{"category":"section","location":"implementation/#Factorized-boundary-propagation","page":"Implementation","text":"After canonicalization at site 1, the sampler maintains the left environment in factorized form:\n\nrho_i = C_i C_i^dagger\n\nFor traced boundary sampling, the initial factor is C_1=I_d_bsqrtd_b, with one column per boundary state. Defining the local slice and propagated factor as\n\nK_i(xy)rell=X_iellxyr\nqquad\nY_i(xy)=K_i(xy)C_i\n\neach physical-site update preserves unit Frobenius norm.","title":"Factorized boundary propagation"},{"category":"section","location":"implementation/#Traced-MPO-mode","page":"Implementation","text":"When computing branch weights, purification outcomes are stacked horizontally as column blocks:\n\nG_i(x)=\nbeginbmatrix\nY_i(x1)  Y_i(x2)  cdots  Y_i(xd_y)\nendbmatrix\n\nThe node caches the bank mathcalG_i=(G_i(1)ldotsG_i(d_x)), yielding branch weights directly:\n\nq_i(x)=lVert G_i(x)rVert_F^2\n=sum_ylVert K_i(xy)C_irVert_F^2\n\nWhen child branches are traversed, the selected factor G_i(x)  sqrtq_i(x) is compressed via LQ decomposition G_i = L_i Q_i whenever the column dimension exceeds the right bond dimension. Because Q_i Q_i^dagger = I,\n\nG_i G_i^dagger = L_i L_i^dagger\n\nreplacing G_i with L_i preserves the environment exactly while capping the rank at the right bond dimension. Storing the uncompressed bank mathcalG_i allows single-pass weight evaluation and reuse across child nodes.","title":"Traced MPO mode"},{"category":"section","location":"implementation/#MPS-and-joint-modes","page":"Implementation","text":"A traced MPS propagates its d_b boundary columns through each selected channel. When the column count exceeds the bond dimension D_i, LQ compression reduces the rank without altering C_i C_i^dagger.\n\nIn joint sampling, the boundary state is sampled at site 1, reducing C_1 to a single column. Subsequent site updates:\n\nC_i+1=fracK_i(xy)C_isqrtq_i(xy)\n\npreserve this rank-one property, scaling as mathcalO(D^2) per contraction. For traced MPS with r le d_b columns, each update costs mathcalO(r D^2). In traced MPO mode, purification legs can increase the rank up to D, yielding mathcalO(D^3) scaling in the worst case.\n\nFor tangent joint sampling, conditioning on the boundary, the global q (if present), and local purification indices maintains a rank-one factor structure with mathcalO(D^2) propagation cost. In traced tangent mode, tracing purification legs similarly requires history compression and scales as mathcalO(D^3).","title":"MPS and joint modes"},{"category":"section","location":"implementation/#Global-modes-and-local-tensor-ranks","page":"Implementation","text":"The input type and purified flag determine the sampling semantics:\n\nInput type Sampled outcomes Factor update\nMPS, purified=true x_i Boundary trace with LQ compression\nMPS, purified=false Boundary b, then x_i Rank-one propagation\nMPO, purified=true x_i Boundary and purification trace with LQ compression\nMPO, purified=false Boundary b, then (x_i y_i) Rank-one propagation\nTangentMPS, purified=true x_i Traced boundary in (U V_q) history\nTangentMPS, purified=false Boundary b, optional q, then local outcomes Rank-one history propagation\n\nAn MPO may contain both rank-three and rank-four tensors. At rank-three sites, contractions treat the purification leg as a singleton and joint sampling sets y_i=0.","title":"Global modes and local tensor ranks"},{"category":"section","location":"implementation/#Compiled-symmetry-contractions","page":"Implementation","text":"Each site is compiled into a SitePlan containing basis metadata, views into reduced TensorMap blocks, allowed sector transitions, and cached fusion-tree kernels. A physical flat index maps to (textsector textdegeneracy textirrep) in canonical TensorKit order.","title":"Compiled symmetry contractions"},{"category":"section","location":"implementation/#Residual-symmetry","page":"Implementation","text":"Projecting onto a local basis state can break part of the global symmetry. The compiler determines the residual symmetry structurally: one-dimensional UniqueFusion components are preserved as residual charges, while non-Abelian carrier indices are absorbed into the row degeneracy of the residual tensor blocks.\n\nOriginal virtual sectors are grouped into residual sector blocks. Transitions sharing the same residual charges form a ChannelRoute, accumulating amplitudes coherently before taking the norm. This preserves block sparsity while accounting for interference between sectors sharing a residual charge.","title":"Residual symmetry"},{"category":"section","location":"implementation/#Contraction-kernels","page":"Implementation","text":"The contraction kernel is determined by BornSampling.TK.FusionStyle(sector_type):\n\nUniqueStyle: Directly contracts one-dimensional irrep carriers via matrix multiplication on reduced blocks.\nFusionTreeStyle: Uses cached fusion-tree arrays to contract degeneracy and irrep coordinates into preallocated scratch buffers.","title":"Contraction kernels"},{"category":"section","location":"implementation/#Layer-synchronous-prefix-reuse","page":"Implementation","text":"Batched sampling advances all shots layer by layer from left to right. Prefixes entering a layer form the current frontier, while selected branches are collected into the next frontier. Each prefix node stores its accumulated log probability, next-site branch weights, and environment tensor. When multiple shots follow the same prefix path, they share the node's branch weights and environment.\n\nWithin each layer, an atomic counter assigns work to worker tasks. In MPS and joint-MPO batches, shots sharing a nonresident parent form one job so its factor is loaded once for the group; shots with resident parents remain individual jobs. Each worker operates with its own contraction workspace, and per-shot random number generators are seeded upfront to ensure reproducibility regardless of task scheduling.\n\nWhen all shots complete a layer, the next frontier becomes active and the preceding frontier is released. Child nodes are published with fine-grained locking: only the first worker to visit an edge constructs the new node, while subsequent workers follow a lock-free path.","title":"Layer-synchronous prefix reuse"},{"category":"section","location":"implementation/#Tangent-suffix-completions","page":"Implementation","text":"Before sampling a tangent batch, a right-to-left sweep builds suffix environments in the original symmetry:\n\nI=R^dagger Rqquad K_q=R^dagger T_qqquad\nN_qq=T_q^dagger T_q\n\nJoint sampling retains both q legs of N during the sweep; traced sampling uses N_mathrmtr=sum_q N_qq. The sweep runs once per batch, with cost determined by the original symmetry's reduced blocks and fusion channels. Each completed suffix is converted to residual-sector matrices for storage, keeping only N_qq in joint mode, while the next transfer uses the original-symmetry environment. Forward sampling starts after all completions are prepared and shares each site completion read-only across worker tasks.\n\nWith disk=false, converted suffix completions are held in memory. With disk=true, they are written directly to temporary files and deleted after consumption, keeping resident suffix memory constant with respect to chain length. Neither mode stores a chain of original-symmetry environments.","title":"Tangent suffix completions"},{"category":"section","location":"implementation/#Probability-ranked-environment-storage","page":"Implementation","text":"Prefix metadata and branch weights remain in memory, while tensor environments can be offloaded to disk. When disk storage is enabled, each frontier retains at most maxsize environments with the highest prefix probabilities in memory; remaining environments are serialized to disk.\n\nBecause frontiers are ranked independently, each layer retains its top-maxsize environments. As shots transition between layers, environments are admitted to memory or written to disk through atomic operations. When a frontier is released at the layer boundary, its temporary files are cleaned up.","title":"Probability-ranked environment storage"},{"category":"section","location":"tutorial/#tutorial","page":"Tutorial","text":"This tutorial demonstrates Born sampling on a half-filled Fermi-Hubbard chain using a ground-state MPS and a thermal purification MPO. We sample physical configurations from the ground state, and explore both the exact physical marginal and the joint physical–purification distribution from the thermal state.\n\nThe example uses a chain of length L=16, U=8, mu=U2, and bond dimension D=512. The calculations demonstrate the workflow rather than full numerical convergence.","title":"Tutorial"},{"category":"section","location":"tutorial/#tutorial_setup","page":"Tutorial","text":"The Hamiltonian is\n\nH = -sum_langle ijranglesigma\nleft(c^dagger_isigmac_jsigma + mathrmhcright)\n+ Usum_i n_iuparrown_idownarrow - musum_i n_i\n\nThe model uses the charge U(1) and spin SU(2) representations from FiniteMPS.jl. Helper functions for state preparation, exact contraction, and plotting are defined in tutorial_helpers.jl.\n\nL = 16\nU = 8.0\nmu = U / 2\nD = 512\nNs_rankone = 1000\nNs_traced = 100\ntraced_batch_count = 10\nntasks = Threads.nthreads()\n\nhamiltonian = hubbard_hamiltonian(L; U, mu)\nsz_lookup = hubbard_sz_lookup()\n4-element Vector{Float64}:\n  0.5\n -0.5\n  0.0\n  0.0\n\nSpin correlations at separation r = i - j are averaged within each shot to compute the sample mean and standard error.","title":"Shared Hubbard setup"},{"category":"section","location":"tutorial/#tutorial_mps","page":"Tutorial","text":"At half filling, total U(1) charge is zero in the particle–hole-symmetric convention. The ground state is prepared via CBE 1-DMRG with bond dimension D.\n\nground_state, ground_result = prepare_ground_state(\n    hamiltonian;\n    nsites=L,\n    D,\n    seed=3101,\n)\nground_result\n(sweeps = 3, final_energy = -69.03713409165402)\n\nBornSampler compiles the contraction plans. Batched sampling returns configurations as matrix columns along with their log probabilities. By default, purified=true traces the boundary.\n\nground_direct = direct_szsz_by_distance(ground_state; ntasks)\n\nground_sampler = BornSampling.BornSampler(ground_state)\nground_batch = BornSampling.bornsample!(\n    Random.MersenneTwister(3102),\n    ground_sampler,\n    Ns_rankone;\n    ntasks,\n)\n\nground_mean, ground_se = sampled_szsz_by_distance(\n    ground_batch.configuration,\n    sz_lookup,\n)\n\nplot_szsz_comparison(\n    ground_direct,\n    ((\n        mean=ground_mean,\n        standard_error=ground_se,\n        color=:royalblue,\n        label=\"MPS samples\",\n    ),);\n    direct_label=\"Ground-state expectation\",\n    filename=\"tutorial_hubbard_ground_state_szsz.png\",\n)\n\n(Image: )\n\nThe sample mean estimates the spin correlation function, with error bars indicating the standard error.\n\nground_batch = nothing\nground_sampler = nothing\nground_state = nothing\nGC.gc()","title":"Ground-state MPS sampling"},{"category":"section","location":"tutorial/#tutorial_mpo","page":"Tutorial","text":"For a purified thermal state Xrangle, setting purified=true traces the boundary and local purification legs to sample from the exact physical marginal:\n\np(x) = fracsum_yb X(bxy)^2langle XXrangle\n\nwhile purified=false samples the joint distribution:\n\np(xyb) = fracX(bxy)^2langle XXrangle\n\nDiscarding purification and boundary indices from joint samples yields the same physical marginal p(x). The thermal state is prepared at beta=1 using SETTN and CBE 1-TDVP.\n\nfactor, beta = prepare_thermal_factor(hamiltonian; D)\nbeta\n1.0","title":"Thermal purification MPO sampling"},{"category":"section","location":"tutorial/#Exact-physical-marginal","page":"Tutorial","text":"We compute the exact expectation value as a reference, then sample physical configurations from the traced marginal distribution. Here, shots are drawn in smaller batches to keep peak memory low.\n\nthermal_direct = direct_szsz_by_distance(factor; ntasks)\n\ntraced_state = deepcopy(factor)\ntraced_sampler = BornSampling.BornSampler(traced_state; purified=true)\ntraced_rng = Random.MersenneTwister(3201)\ntraced_configuration = Matrix{Int}(undef, L, Ns_traced)\ntraced_batch_size = Ns_traced ÷ traced_batch_count\n\nfor batch in 1:traced_batch_count\n    columns = ((batch - 1) * traced_batch_size + 1):(batch * traced_batch_size)\n    result = BornSampling.bornsample!(\n        traced_rng,\n        traced_sampler,\n        traced_batch_size;\n        ntasks,\n    )\n    traced_configuration[:, columns] .= result.configuration\nend\n\ntraced_mean, traced_se = sampled_szsz_by_distance(\n    traced_configuration,\n    sz_lookup,\n)\n\ntraced_configuration = nothing\ntraced_sampler = nothing\ntraced_state = nothing\nGC.gc()","title":"Exact physical marginal"},{"category":"section","location":"tutorial/#Joint-physical–purification-samples","page":"Tutorial","text":"Joint sampling returns a (2L+1)times N_s configuration matrix ordered as x_1ldotsx_Ly_1ldotsy_Lb, with b=1 for the singlet boundary. The first L rows correspond to physical indices and are passed directly to the correlation estimator.\n\njoint_sampler = BornSampling.BornSampler(factor; purified=false)\njoint_batch = BornSampling.bornsample!(\n    Random.MersenneTwister(3202),\n    joint_sampler,\n    Ns_rankone;\n    ntasks,\n)\njoint_physical_configuration = @view joint_batch.configuration[1:L, :]\njoint_mean, joint_se = sampled_szsz_by_distance(\n    joint_physical_configuration,\n    sz_lookup,\n)\n\njoint_summary = (;\n    joint_configuration_size=size(joint_batch.configuration),\n    samples=Ns_rankone,\n)\njoint_summary\n(joint_configuration_size = (33, 1000), samples = 1000)\n\nWe compare the exact thermal expectation against both traced sampling and the physical marginal of joint sampling.\n\nplot_szsz_comparison(\n    thermal_direct,\n    (\n        (\n            mean=traced_mean,\n            standard_error=traced_se,\n            color=:royalblue,\n            label=\"Trace (×$(Ns_traced))\",\n        ),\n        (\n            mean=joint_mean,\n            standard_error=joint_se,\n            color=:darkorange,\n            label=\"Joint (×$(Ns_rankone))\",\n        ),\n    );\n    direct_label=\"Thermal expectation\",\n    filename=\"tutorial_hubbard_thermal_szsz.png\",\n)\n\n(Image: )","title":"Joint physical–purification samples"}]
}
